using System;
using CognitiveServices;
using CognitiveServices.iOS;
using Xamarin.Forms;
using Xamarin.Forms.Platform.iOS;
using AVFoundation;
using CoreGraphics;
using Foundation;
using UIKit;


//[assembly: ExportRenderer(typeof(CreditPage), typeof(CreditPageRender))]
namespace CognitiveServices.iOS
{
    public class CreditPageRender //: PageRenderer
    {
      
      /*  protected override void OnElementChanged(VisualElementChangedEventArgs e)
        {
            base.OnElementChanged(e);
            
            if (e.OldElement != null || Element == null)
            {
                return;
            }

            try
            {
                SetupUserInterface();
                
            }
            catch (Exception ex)
            {
                System.Diagnostics.Debug.WriteLine(@"            ERROR: ", ex.Message);
            }
        }

        private void SetupUserInterface()
        {
            //var view = CreditView.Instance();
            //view.Layer.ZPosition = 999;

            //CGRect rect = new CGRect(0, 0, 300, 300);
            //view.Frame = rect;
            //this.View = view;
             
        }*/
    }
}
